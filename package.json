{
  "name": "local-cam-recommender-root",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write .",
    "format:backend": "backend/venv/Scripts/python -m black backend/"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx,json,css,md}": "prettier --write",
    "backend/**/*.py": "backend/venv/Scripts/python -m black"
  }
}
